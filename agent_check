pipeline{
  agent none
   stages {
     stage ('BUILD'){
       agent { label 'master' }
         steps{
           echo " this the build which is ran on agent master "
             sh 'sleep 5'
        }
      }
    }
}
